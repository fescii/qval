// Impoert all the queries from the users and export them
const userEditQueries = require('./edit.user');
const userActionQueries = require('./action.user');

module.exports = {
  userEditQueries, userActionQueries
};
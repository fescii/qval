// Importing all routes and exporting them as a single object
const joinPages = require('./join.pages');
const topicPages = require('./topic.pages');
const userPages = require('./user.pages');

module.exports = {
  joinPages, topicPages, userPages
}